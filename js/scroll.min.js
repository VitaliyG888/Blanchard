document.addEventListener("DOMContentLoaded", function () {
	"use strict";
	let n = function () {
		function u(n) {
			let t;
			return [].forEach.call(i, function (i, r) {
				i === n && (t = r)
			}), t
		}

		function f(n) {
			[].forEach.call(r, function (t, i) {
				if (i == n) {
					let i = t.getBoundingClientRect().top,
						n = i > 0 ? 1 : 0;
					if (n == 0) return;
					e(t, n)
				}
			})
		}

		function e(t, i) {
			let u = 5e3,
				f = (new Date).getTime(),
				r = function () {
					let o = t.getBoundingClientRect().top,
						s = (new Date).getTime() - f,
						e = Math.round(o * s / u);
					e = e > i * o ? o : e == 0 ? i : e;
					i * o > 0 && (window.scrollBy(0, e), n(r))
				};
			n(r)
		}
		let n = window.requestAnimationFrame || window.mozRequestAnimationFrame || window.webkitRequestAnimationFrame || window.msRequestAnimationFrame;
		window.requestAnimationFrame = n;
		let t = document.querySelector(".header__nav"),
			i = t.querySelectorAll(".nav-prime__link"),
			r = document.querySelectorAll("h2.title"),
			o = Math.max(document.body.scrollHeight, document.documentElement.scrollHeight, document.body.offsetHeight, document.documentElement.offsetHeight, document.body.clientHeight, document.documentElement.clientHeight);
		t.onclick = function (n) {
			if (n.target.tagName == "A") {
				let t = u(n.target);
				f(t)
			}
		}
	};
	n()
})
