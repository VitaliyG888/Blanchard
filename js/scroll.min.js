document.addEventListener("DOMContentLoaded", function () {
	(function () {
		function k(a) {
			var b;
			[].forEach.call(l, function (e, c) {
				e === a && (b = c)
			});
			return b
		}

		function m(a) {
			[].forEach.call(n, function (b, e) {
				if (e == a) {
					var c = 0 < b.getBoundingClientRect().top ? 1 : 0;
					0 != c && p(b, c)
				}
			})
		}

		function p(a, b) {
			var e = (new Date).getTime(),
				c = function () {
					var f = a.getBoundingClientRect().top,
						d = (new Date).getTime() - e;
					d = Math.round(f * d / 5E3);
					d = d > b * f ? f : 0 == d ? b : d;
					0 < b * f && (window.scrollBy(0, d), g(c))
				};
			g(c)
		}
		var g = window.requestAnimationFrame || window.mozRequestAnimationFrame ||
			window.webkitRequestAnimationFrame || window.msRequestAnimationFrame;
		window.requestAnimationFrame = g;
		var h = document.querySelector(".header__nav"),
			l = h.querySelectorAll(".nav-prime__link"),
			n = document.querySelectorAll("h2.title");
		h.onclick = function (a) {
			"A" == a.target.tagName && (a = k(a.target), m(a))
		}
	})()
});
